import { DraganddroplistDirective } from './draganddroplist.directive';

describe('DraganddroplistDirective', () => {
  it('should create an instance', () => {
    const directive = new DraganddroplistDirective();
    expect(directive).toBeTruthy();
  });
});
